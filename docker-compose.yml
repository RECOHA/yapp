version: '3.8'

services:
  api:
    build: .
    container_name: yapp-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./downloads:/app/downloads
      - ./processed:/app/processed
    environment:
      - PORT=3000
      - NODE_ENV=production
